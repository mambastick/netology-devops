apiVersion: v1
kind: Service
metadata:
  name: multiport-svc
spec:
  selector:
    app: multiport-app
  ports:
    - name: nginx
      port: 9001
      targetPort: 80
    - name: multitool
      port: 9002
      targetPort: 8080
