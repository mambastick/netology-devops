apiVersion: v1
kind: Pod
metadata:
  name: rbac-test-nginx
  namespace: kuber-2-4
  labels:
    app: rbac-test-nginx
spec:
  containers:
    - name: nginx
      image: nginx:1.25.5
      ports:
        - containerPort: 80
